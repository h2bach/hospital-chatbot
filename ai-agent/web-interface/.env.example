# Development: Vite chuyển tiếp /c và /mcp tới backend Go.
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Để trống theo mặc định để frontend gọi API cùng origin.
# Chỉ bật khi frontend và backend khác origin, đồng thời backend đã cấu hình CORS.
# Giá trị này được nhúng vào bundle tại thời điểm npm run build:static.
# VITE_API_BASE_URL=https://api.example.vn

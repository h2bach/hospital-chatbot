services:
  mock-info-service:
    build:
      context: ./mock-info-service
    command: ["--port", "8080", "--data", "/app/data"]
    volumes:
      - ./mock-data/output:/app/data:ro
    ports:
      - "8081:8080"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:8080/health"]
      interval: 5s
      timeout: 3s
      retries: 10

  ai-agent:
    build:
      context: ./ai-agent
    env_file:
      - .env
    environment:
      PORT: "8080"
      AGENT_STATIC_DIR: /app/static
      MOCK_INFO_SERVICE_URL: http://mock-info-service:8080
    depends_on:
      mock-info-service:
        condition: service_healthy
    ports:
      - "8080:8080"
